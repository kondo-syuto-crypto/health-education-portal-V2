{
  "name": "health-education-portal",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite dev",
    "preview": "wrangler pages dev dist",
    "dev:d1": "wrangler pages dev dist --d1=health-education-portal-db --local --ip 0.0.0.0 --port 3000",
    "db:migrate:local": "wrangler d1 migrations apply health-education-portal-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply health-education-portal-db"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@hono/vite-cloudflare-pages": "^0.4.2"
  }
}